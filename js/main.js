var cvs = document.getElementById("cvs");
var ctx = cvs.getContext("2d");

var screenSizeX = 640;
var screenSizeY = 480;

var tileSize = 16;

var tilesX = screenSizeX / tileSize;
var tilesY = screenSizeY / tileSize;
